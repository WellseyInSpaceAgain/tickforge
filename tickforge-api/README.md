# Tickforge API

Stable contracts and data models for the Tickforge RuneLite helper framework.

This module may depend on `runelite-api`, but it must not contain RuneLite
plugin implementations, Swing UI, overlays, sidebar panels, or other
`runelite-client` implementation details.